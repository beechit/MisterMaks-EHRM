(function() {
	'use strict';

	App.datePickerSettings = {
		showOn: 'focus',
		minView: 3
	};

	App.dateTimePickerSettings = {
		minuteStep: 15,
		showOn: 'focus',
		autoclose: true
	};

}).call(this);